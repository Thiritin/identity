on:
  push:
    branches: [ main ]
  release:
    types:
      - created
jobs:
  build-docker:
    uses: thiritin/workflows/.github/workflows/docker.yml@main
    secrets:
      DockerHubUser: ${{ secrets.DOCKERHUB_USERNAME }}
      DockerHubPass: ${{ secrets.DOCKERHUB_PASSWORD }}
